import * as React from 'react';

import styled, { injectGlobal, ThemeProvider, THEME, withTheme } from 'src/modules/styled-components'
import { Layout } from 'antd'
export const NavLinkStyled = styled(Layout.Content) `

`;
